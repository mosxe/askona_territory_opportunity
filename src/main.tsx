import React from 'react';
import ReactDOM from 'react-dom/client';
import OpportunityPage from './pages';
import './index.css';

ReactDOM.createRoot(document.getElementById('territory_opportunity')!).render(
  <React.StrictMode>
    <OpportunityPage />
  </React.StrictMode>
);
